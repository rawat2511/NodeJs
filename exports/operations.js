const add = (a, b) => a + b;

const sub = (a, b) => a - b;

const mul = (a, b) => a * b;

const div = (a, b) => a / b;

const name = "Shubham";

module.exports = {
  add,
  sub,
  mul,
};

module.exports.div = div;
module.exports.name1 = name;
